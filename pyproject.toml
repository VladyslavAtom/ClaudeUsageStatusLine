[project]
name = "claude-usage-status"
version = "1.0.0"
description = "Claude Usage Status Fetcher - Retrieves usage data from Claude API"
requires-python = ">=3.10"
dependencies = [
    "curl_cffi>=0.14.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0.0",
]
